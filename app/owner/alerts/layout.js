export const metadata = {
  title: 'Alerts | SAFAR',
  description: 'Fleet safety alerts and notifications',
  icons: { icon: '/favicon.png' },
};

export default function AlertsLayout({ children }) {
  return children;
}
