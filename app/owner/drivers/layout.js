export const metadata = {
  title: 'Drivers | SAFAR',
  description: 'Manage and monitor your fleet drivers',
  icons: { icon: '/favicon.png' },
};

export default function DriversLayout({ children }) {
  return children;
}
