export const metadata = {
  title: 'Dashboard | SAFAR',
  description: 'Owner fleet dashboard for live tracking and management',
  icons: { icon: '/favicon.png' },
};

export default function OwnerLayout({ children }) {
  return children;
}
