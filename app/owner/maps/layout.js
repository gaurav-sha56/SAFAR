export const metadata = {
  title: 'Fleet Map | SAFAR',
  description: 'Live fleet map and driver locations',
  icons: { icon: '/favicon.png' },
};

export default function MapsLayout({ children }) {
  return children;
}
