export const metadata = {
  title: 'Reports | SAFAR',
  description: 'Fleet analytics and safety reports',
  icons: { icon: '/favicon.png' },
};

export default function ReportsLayout({ children }) {
  return children;
}
