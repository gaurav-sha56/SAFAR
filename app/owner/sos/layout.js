export const metadata = {
  title: 'SOS | SAFAR',
  description: 'Emergency SOS alerts and response management',
  icons: { icon: '/favicon.png' },
};

export default function SosLayout({ children }) {
  return children;
}
